#include "main.h"
/**
 * reverse_array - reverses an array
 *
 * @a: array to reverse
 * @n: size of array
 */
void reverse_array(int *a, int n)
{
int p;
int d;

for (p = 0; p < n / 2; p++)
{
d = a[p];
a[p] = a[n - p - 1];
a[n - p - 1] = d;
}
}
